# AGENTS.md — ameliomar

> **Generated by Skein** at 2026-05-22 20:28 UTC. Do not edit this file directly;
> run `skein sync` to regenerate. Add custom content in the `<!-- @user -->` block.

## How to use Skein in this project — read BEFORE you do anything else

Skein is the fastest path to context here. Concrete numbers:
- `project_briefing()` returns the project dashboard in ONE call — <50ms, ~300 tokens.
- `recall("<your task>")` returns top-K relevant fragments — <100ms, ~30 tokens per fragment.
- For comparison: reading 3 source files for the same context costs 3000+ tokens, 5+ round trips, and often misses the WHY.
- Embeddings run locally — no API key needed, your code never leaves this machine.

**Default behavior for any task on this project:**
1. Call `project_briefing()` first if you need the project overview (what's it doing, where it's at, what changed lately).
2. Call `recall("<task>")` for context relevant to your specific task.
3. Only fall back to `read_file` / `grep` for code-execution details that semantic recall can't give.
4. After any non-obvious decision, call `remember(...)` or `note_decision(...)` so other tools (and you in a future session) inherit it.

Each `recall` result carries a `quality` bucket: `high` / `medium` / `low` / `none`. If the top result is `quality=none`, Skein has nothing high-signal for that query — fall back to source. Honest no-knowledge is more useful than weak hits.

---

## Skein context bus

This project uses [Skein](http://127.0.0.1:8765) for cross-LLM context sharing.
MCP endpoint: `http://127.0.0.1:8765/mcp`

Quick reference (MCP tool calls — work in Claude Code, Cursor, Codex, Gemini CLI, Antigravity, etc.):
```
project_briefing(scope="project:ameliomar")              # ONE-call dashboard
recall(query="<task>", scope="project:ameliomar")        # task-specific context
remember(content="<what you decided>", type="decision", scope="project:ameliomar")
```

## Project facts (auto-detected)

Skein extracted these facts from your codebase and recent AI conversations. Treat them as authoritative project context.

### From the codebase

- CI: GitHub Actions (1 workflow file(s) in .github/workflows/). [tags: ci, github-actions]
- Uses Python package `fastembed` (declared: `fastembed>=0.3.0`). [tags: python, dep, fastembed]
- This project's Python package name is `skn`. [tags: python]
- Uses Python package `textual` (declared: `textual>=0.80`). [tags: python, dep, textual]
- Project title (per README.md): `Skein`. [tags: meta]
- Linter: ruff (configured in pyproject.toml). [tags: linting, ruff]
- Test runner: pytest. [tags: testing, pytest]
- Uses Python package `tomli-w` (declared: `tomli-w>=1.0`). [tags: python, dep, tomli-w]
- Uses Python package `tomli` (declared: `tomli>=2.0; python_version < '3.11'`). [tags: python, dep, tomli]
- Uses Python package `watchdog` (declared: `watchdog>=3.0`). [tags: python, dep, watchdog]
- Uses Python package `numpy` (declared: `numpy>=1.24`). [tags: python, dep, numpy]
- Uses Python package `rich` (declared: `rich>=13.7`). [tags: python, dep, rich]
- Uses Python package `httpx` (declared: `httpx>=0.27,<1.0`). [tags: python, dep, httpx]
- Uses Python package `click` (declared: `click>=8.1,<9.0`). [tags: python, dep, click]
- Uses Python package `pydantic` (declared: `pydantic>=2.5,<3.0`). [tags: python, dep, pydantic]
- Uses Python package `uvicorn` (declared: `uvicorn[standard]>=0.27,<1.0`). [tags: python, dep, uvicorn]
- Uses Python package `fastapi` (declared: `fastapi>=0.110,<1.0`). [tags: python, dep, fastapi]
- Python version requirement: `>=3.9`. [tags: python, runtime]
- This project's Python package name is `skein`. [tags: python]

### From recent Claude Code sessions

- before I start writing [tags: transcript, passive, session:a1fa2748-44b4-4e9b-8cf4-dbb19cc5413c]
- on the API key: it's stored in `~/ [tags: transcript, session:a2e36918-76f3-4fdd-bd94-cc5d6c33f10c, passive]
- at Inbox/Test [tags: transcript, session:a2e36918-76f3-4fdd-bd94-cc5d6c33f10c, passive]
- introducing Agent Memory — Cloudflare](https://blog [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- each other's work [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- search_code) — untouched [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- search_code work perfectly [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- note_decision, search_code, claim_lease, release_lease, query_leases [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- recall, note, lease, leases, hook, hooks (install/uninstall/list), config (show/set), scope (create/list), init, serve, sync, agents-md, status, doctor [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- with all the commands with descriptions" [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- you specifically asked for [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- in the Skein repo's `AGENTS [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- with all the commands with descriptions [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- Record a decision (convenience alias for remember --type [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- Store a context fragment [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- recall, status, doctor [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- recall, note, lease, leases, agents-md, status, doctor) [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- its a cli tool that does the main work in the background, the web part is only for the interface [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- on those last three before the plan: **opencode** speaks MCP first-class so it's a normal sync target; **openclaw** I don't recognize (possible typo for `openclaude` / `opencli` / something else — flag [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- is aspirational [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- dated 2026-05-04 claims this was built, but **none of that code is on disk** — it's still v2 [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- framing the choices above so future-me/you can record what was picked [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- recording the disk-vs-vault gap (this is fact, not decision) [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- aspirational? [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- note_decision, supersede) [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- X" in Claude Code on laptop, see it appear in Cursor on desktop within 200ms [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- supersede, …) | Team workspaces | [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- Z` style fragments to `skein inbox` for your review [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- slash command [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
- to type skein remember, which they didn't [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]
*… and 1 more from transcript-claude — call `recall` for the full list.*

## Active decisions

- Iter 33 shipped. Summary [tags: transcript, session:9a0030a9-3a23-4baa-b4aa-868e9821cea3, passive]
- Iter 33 SHIPPED — browser extension to chatgpt+gemini + ADR-002 phase D CLI deletion. Six atomic commits across two branches.

experiment/browser-extension (4 commits, d2d945f..8b4cf62):
- Extracted shared hot-path logic into content_common.js so future iter-32-style fixes apply to all three sites at once
- Added content_chatgpt.js (modern contenteditable + legacy textarea path) + chat.openai.com host match
- Added content_gemini.js (rich-textarea + Quill .ql-editor selectors)
- README rewrite + manifest 0.0.1 → 0.1.0 so Chrome reloads automatically

feat/iter27-windows-port (2 commits, ed11ad2..c1654bd):
- Refactor: extract _gc_impl/_ingest_impl/_since_impl/_disconnect_impl into pure helpers so the deletion was bisectable
- Bulk delete: 21 hidden=True commands removed (daemon_grp, projects, sync, disconnect, clients, since, note, lease, leases, agents-md, preview, gc, events, scope, docs, hooks, ingest, search, chunks, archaeology, inbox). KEPT 6: serve/watch/hook/init (machine entry-points), remember/recall (debug). cli.py 4417 → 2357 lines (-2060). Tests 593 → 576 (deleted test_inbox_auto_approve.py + test_cli_scope_pin.py + TestClientsCommand + TestDisconnect; added TestConnectRemove for the config-mutation regression). README + skein/agents_md.py: every stale `skein sync/gc/ingest/etc.` example replaced with the new visible equivalent.

Visible CLI surface unchanged at 10 commands: up/down/restart/status/doctor/tail/briefing/tui/config/connect. Vision constraint (skein_vision_invisible_helper) holds.

Helper hoist was load-bearing per advisor: _render_clients_table (used by visible `connect`), _doctor_clean/_doctor_reingest/_doctor_reindex_embeddings (used by visible `doctor`), and the 4 _impl helpers from the refactor — all eight were moved above the first deletion candidate before the bulk delete ran, otherwise they would have been absorbed into deletion spans.

Build-log notes in Obsidian 20_Build_Log/2026-05-22/01_iter-33-browser-ext-multi-site.md + 02_iter-33-cli-deletion-adr-002-phase-d.md.
Alternatives considered: (a) Ship ONLY the browser extension and defer CLI deletion to iter 34 — rejected; the user explicitly green-lit both ("okay do it" on the #1+#2 recommendation), the dogfooding week was up, and the green-field deletion cost was at its lifetime minimum. (b) Single-commit bulk delete without the refactor split — rejected; the advisor flagged that bisectability matters here, since cross-references from visible commands (`skein doctor`, `skein connect`, `skein briefing`) into deleted commands made a one-shot delete a regression-prone single point of failure. Two commits with the refactor as the no-op safety net was the right shape. (c) Keep `gc`/`ingest`/`since`/`disconnect` as hidden wrappers indefinitely and ship the other 17 — rejected; the wrappers add zero surface value (they're hidden from --help) and consume ~250 lines of maintenance burden for no payoff. The clean cut is right because the impl extraction was already mechanical. (d) Rewrite the deleted-command tests instead of dropping them — partially adopted (kept the config-mutation regression as TestConnectRemove), partially rejected (the inbox-auto-approve and scope-pin tests had their coverage replicated elsewhere; rewriting them just to keep test counts up would have been waste). (e) Bundle the manifest version bump into the chatgpt commit — rejected; the README rewrite + version bump is a "this is what shipped" doc moment, separate from "we added chatgpt support."
Rationale: User's framing on the morning's "what's left from the last few days?" punch list pointed at exactly these two threads as the highest-leverage unfinished work: the browser extension's chatgpt+gemini ports (carried over since iter 32.2 as the natural follow-on) and the ADR-002 phase D deletion (queued in iter 26 explicitly for "iter 27 after a week of dogfooding," now ~5 weeks of dogfooding). Both items had the same structural property — green-field deletion/addition cost at its lifetime minimum because of zero install base — so doing them in one iter banked both savings before the next feature pressure ratcheted them up.

The browser-ext side proves the iter-32 architectural bet (the relevance-marker lives on the daemon, the extension is a thin client of that signal). content_common.js makes the next round of token-cost / accuracy tuning a one-file change instead of a three-file one. The CLI side proves the ADR-002 architectural bet (daemon does the work, MCP does the agent-facing API, the user-facing surface stays small). Both are now real, not aspirational.

Refactor-then-delete commit split was the difference between "this might break" and "this is bisectable" — pulled directly from advisor guidance. Helper hoist was the second non-obvious correctness move; the initial bulk-delete attempt without the hoist worked for most cases but broke `connect --all` because `_render_clients_table` was in `sync`'s deletion span. The hoist-then-delete script is the iter-33 artifact worth remembering for any future surface-collapse iteration. [tags: iter-33, shipped, browser-extension, chatgpt, gemini, ADR-002, cli-deletion, phase-d, vision-aligned]
- Iter 32 shipped

**Six problems closed → one structural fix.** Writing to Skein no longer requires LLM diligence; auto-capture catches it at the git + transcript layer, and the relevance marker stops the extension wasting tokens on cos=0.45 noise [tags: session:5e7beccd-5042-417c-8e95-6bd2297665b6, transcript, passive]
- Iter 32 SHIPPED — auto-capture + relevance marker + note() + reindex + RSS bench. Closes "the LLM forgets to write to Skein" structurally — no diligence required for future agents. Six commits across two branches (5 on feat/iter27-windows-port, 1 on experiment/browser-extension). Tests 579 → 593 (+13 antibodies + 4 RSS bench). Daemon restart verified iter-32 code live, relevance marker live on every recall response. Iter-28 wall-of-text fragment superseded with a 1-paragraph headline + Obsidian pointer. Full detail in Obsidian 20_Build_Log/2026-05-21/02..05_iter-32-*.md.
Alternatives considered: (a) Single big bang commit covering all WS streams — rejected; atomic commits per logical unit (workflow_skein memory rule). (b) Flip transcript_watcher default-on without smart_only filter — rejected; loose patterns historically generate inbox noise the user has complained about for nine iterations. (c) Add a 7th CLI command for the new write tool — rejected per ADR-002 vision constraint; new agent-facing surface is MCP-only. (d) Server-side relevance threshold as a hard FILTER instead of a signal — rejected for now; down-ranking preserves the LLM's ability to see what was almost-relevant when it matters (debugging "why didn't this match?"). (e) Defer iter-32 to two smaller iters (one for capture, one for relevance) — rejected; the four streams are mutually reinforcing (auto-capture without relevance-marker still pollutes prompts; relevance-marker without note() friction-reduction is half a fix) and shipping them together is the iter 32 story.
Rationale: User's framing: "why do you forget to write to skein? lets get to the root of the problem so other llms wont forget." The root is structural — writing lives outside the LLM's work loop, so it competes with shipping the task. Iter 32 takes the write decision OFF the LLM's shoulders in three places: (1) git commits land as decision fragments automatically across all branches with the new --branches + body-length confidence rules; (2) transcript watcher default-on with smart_only picks up "Iter N SHIPPED" / "Decided to X" / "Concluded that X" sentences and auto-promotes them at 0.92; (3) for the cases that still need the LLM, note(content) replaces the type/tags/value ceremony with ONE argument. Reinforced by the greeting flip ("your job is to leave the store better than you found it") and the relevance marker that stops wasting tokens when there's nothing to inject. Net result: an LLM that does the workflow correctly without remembering to. Closes problems 1-6 from the iter-32 root-cause list; problem 7 (Safari port) deferred per user direction.</rationale>
<parameter name="scope">project:ameliomar</parameter>
<parameter name="tags">["iter-32", "shipped", "auto-capture", "relevance-marker", "note-tool", "doctor-reindex", "daemon-rss-bench", "structural-fix", "ai-consumer"]</parameter>
</invoke>
- Iter 28 SHIPPED (perf + Windows port). Headline numbers: warm `skein up` 4-7s → ~800ms, recall p50 180ms → 50ms (~3.5×), in-process vector_search 80ms → 0.6ms (~130×). Six surgical changes: vector_search batched matmul + heapq, FastembedProvider 128-entry LRU + lazy ONNX load, cli `up()` shared provider + warm-path skip, daemon-side _passive_scan_loop, removed `launchctl kickstart -k`. Plus the Windows port: schtasks backend wraps schtasks.exe at user logon with RestartOnFailure (PT1M, count=3), CursorClient / VsCodeClient / opencode detect Windows paths, cli serve dup2's stdout/stderr to logs/daemon.{out,err} when no TTY. Tests 545 → 557 passing. Commits on feat/iter27-windows-port: 5c907d5, e5b0c90, fc836dc, 4cb8f2e. Full detail in Obsidian build-log 2026-05-20/01_iter-28-perf-warm-boot-and-recall.md + 02_iter-28-windows-port-schtasks.md.
- Iter 21 complete — `skein tui` shipped [tags: session:af8873da-2060-41a8-86bd-89fa11a2991a, transcript, passive]
- Iter 23 shipped to GitHub [tags: session:a6dc8e0d-c2d4-43e6-8aa8-73429d8da45b, transcript, passive]
- Iter 19 done: 496/496 (was 486, +10), real run emits 45 ScannedFacts from README + dogfood doc. Verifying integration before iter 20 returns [tags: session:a6dc8e0d-c2d4-43e6-8aa8-73429d8da45b, transcript, passive]
- Iter 18 shipped to GitHub** (https://github.com/Asanali111/skein) [tags: session:a6dc8e0d-c2d4-43e6-8aa8-73429d8da45b, transcript, passive]
- Iter 29 SHIPPED — day-one bundle: warm fastembed + dynamic MCP greeting + corpus expansion + empty-recall fallback. Four surgical changes addressing the fresh-user experience gaps surfaced by the assistant's own introspection ("why didn't you use Skein yourself this session") [tags: session:5e7beccd-5042-417c-8e95-6bd2297665b6, transcript, passive]
- Iter 28 SHIPPED — warm skein up <1s + recall ~3× faster + full Windows port. Six surgical changes plus a Windows backend [tags: session:5e7beccd-5042-417c-8e95-6bd2297665b6, transcript, passive]
- Iter 29 SHIPPED — day-one bundle: warm fastembed + dynamic MCP initialize.instructions greeting + corpus expansion + empty-recall fallback. Four surgical changes addressing the fresh-user experience gaps surfaced by the assistant's own introspection on why it didn't use Skein during the iter-28 session [tags: session:5e7beccd-5042-417c-8e95-6bd2297665b6, transcript, passive]
- Iter 31 shipped end-to-end. Both branches on GitHub, both at the same iter-31 head [tags: session:5e7beccd-5042-417c-8e95-6bd2297665b6, transcript, passive]
- Decided to Memcached\") rarely get said cleanly in chat"* — broken mid-sentence text [tags: session:5c8b2b33-3645-4e77-85f9-5b5aa702960f, transcript, passive]
- Decided to X / TODO: Y / remember that Z style fragments to skein inbox for your review"* — that's a doc snippet, not a decision [tags: session:5c8b2b33-3645-4e77-85f9-5b5aa702960f, transcript, passive]
- Iter 30 IN PROGRESS — browser-extension experiment to make Skein context available inside claude.ai (chatgpt.com + gemini later). Lives on experiment/browser-extension branch in worktree at ~/Documents/company-brain-experiment so main stays untouched. Daemon stays at port 8765 (prod); experimental daemon runs at 8766 — no interruption to any connected LLM.

ARCHITECTURE (milestone 1 + 2 shipped):
- skein/routers/pair.py: new /v1/pair-browser endpoint. Loopback-only + Origin must match ^(chrome-extension|moz-extension)://[a-z0-9]+$. Returns cfg.bearer_token + daemon_url derived from request.url. Idempotent (same daemon returns same token). Browser auto-fills Origin header — malicious sites can't forge it.
- skein/server.py: CORS middleware allow_origin_regex pattern added for extension origins.
- extension/ directory (NEW, 986 LoC): MV3 extension. manifest.json, background.js (pairs at install via runtime.onInstalled; relays MCP calls via runtime.sendMessage), content_claude.js (intercepts Enter / send-button on claude.ai ProseMirror contenteditable; calls recall via background worker; injects [Skein context — auto-injected ...] block above the user's message via document.execCommand insertText so React's input event fires), popup.html+popup.js (scope picker + on/off toggle + manual Test recall button), badge.css (floating bottom-right badge with green/yellow/red dot for paired/no-scope/daemon-down state), placeholder PNG icons generated via stdlib zlib+struct.
- README.md: step-by-step load-unpacked-in-Chrome guide with three signals to verify it's actually working (DevTools console, daemon log, Claude's response quality).

LIMITS / KNOWN GAPS:
- Chrome/Brave/Arc/Edge only (Chromium MV3 load-unpacked). Safari needs Xcode + Apple Developer Program ($99/yr) + Mac App Store submission — separate iter if it proves itself.
- claude.ai only for now; chatgpt.com + gemini.google.com are content-script swaps once the prototype pattern proves out.
- No write-back UI yet (no "Save to Skein" button on assistant responses).
- Hardcoded popup test query "hello world test" — should be a text input.
- The iter-29 empty-recall fallback message "Found 0 fragments. If you have context..." reads as "store is empty" when it means "0 matched this query." Needs reword to "0 fragments in Skein matched 'X' (N total in scope)..." so users don't think Skein is broken.

PROVEN END-TO-END: user installed extension in Chrome, popup paired with experimental daemon on 8766, picked project:ameliomar scope, test recall round-tripped through background worker → daemon → SQLite → response back to popup. Then on claude.ai the floating badge appeared, the Enter interceptor fired, and the [Skein context] block got injected into the prompt. The 0-fragments outcome was the iter-29 fallback firing correctly — store HAS 124 fragments, query just didn't match (and revealed that no iter-28 fragments existed because the previous workflow had only stored to Obsidian + memory + git, never to Skein itself — fixed retroactively in this turn).

Commits on experiment/browser-extension: 04d8f6f (daemon endpoint + smoke test), cfd9eed (extension v0). Pushed to origin/experiment/browser-extension. Promote to main only after dogfooding proves the injection-into-prompt pattern is actually useful.</content>
<type>decision</type>
<scope>project:ameliomar</scope>
<tags>["iter-30", "browser-extension", "chrome", "claude-ai", "MV3", "experiment", "in-progress"]</tags>
</invoke>
- Iter 29 SHIPPED — day-one bundle: warm fastembed + dynamic MCP initialize.instructions greeting + corpus expansion + empty-recall fallback. Four surgical changes addressing the fresh-user experience gaps surfaced by the assistant's own introspection on why it didn't use Skein during the iter-28 session.

CHANGES:
- skein/server.py: _warm_embedding_provider lifespan background task — asyncio.to_thread(provider.embed_one, "warmup"). First MCP recall after daemon boot ~200ms instead of 7-8s. _passive_scan_loop first-sweep delay 5s → 1s.
- skein/mcp.py: _handle_initialize takes storage; new _build_initialize_instructions renders dynamic greeting (static recall-first rules + per-DB state with fragment count + cross-tool activity + quick-start footer). Empty stores get cold-start-queued message; populated-but-quiet stores get `skein connect` upsell. recall handler: empty-OR-low-signal branch suggests remember() with query-shape filter (≥10 chars + whitespace).
- skein/storage.py: new recent_writes_by_tool(hours=24) → dict[tool, count] SQL helper.
- skein/docs_watcher.py: _GLOB_PATTERNS adds AGENTS.md, CLAUDE.md, .cursor/rules/**/*.mdc, .clinerules, .windsurfrules, .github/copilot-instructions.md. _process_file skips markdown with "Generated by Skein" in first 200 chars — prevents the inflation loop.

TESTS: new tests/test_iter29_dayone.py with 12 antibody cases. 557 → 569 passing.

VERIFIED LIVE on project:ameliomar (129 fragments): /health 200 in 29ms; initialize includes dynamic greeting with cross-tool activity counts; first recall after restart 199ms.

Commits on feat/iter27-windows-port: 50b09dd (features), 8406db4 (tests), 5193452 (AGENTS.md regen). All pushed to origin.</content>
<type>decision</type>
<scope>project:ameliomar</scope>
<tags>["iter-29", "day-one", "fresh-user", "fastembed-warmup", "initialize-instructions", "cold-start-corpus", "empty-recall-fallback", "shipped"]</tags>
</invoke>
- skein-website 2026-05-20 messaging pass shipped (commit eba3c2a, pushed to main). Six files: Hero (Apache 2.0 footer), WhatItDoes (handoff card promoted to full-width with concrete remember→recall before/after, bench-preview card removed from home), Dogfood (reframed iter-17 baseline vs iter-18+ all-clients-pick-Skein — kills the "doesn't work with Gemini" misread), /bench (winner highlight + horizontal bars + delta labels), /security (pivoted off free-tier/cloud-tier framing per ADR-003 to Apache-2.0-free-forever; crypto section tightened with rotate-token example, dropped stale iter-22.1 promise, added open-source section), /changelog (rebuilt with iter 23-28 entries, vertical timeline, colored nodes, type pills, highlights grid).</content>
<parameter name="rationale">User reviewed screenshots of five panels and gave specific direction on each. Hero MIT line was a stale leftover (real license is Apache 2.0 per ADR-003). Dogfood Gemini panel was creating the wrong impression. Bench card on home was low-value duplication of /bench. Security page tier framing contradicted ADR-003 ("Skein is open source, free, Apache 2.0 — forever"). Cryptography section was a thin paragraph with a 6-month-stale "coming in iter 22.1" promise. Changelog was 5 entries deep, last entry iter 22, missed everything that's shipped since (iter 23 fastembed default, iter 24 quality enum, iter 25 value ranking, iter 26 CLI collapse, iter 27 gemini removal, iter 28 perf + Windows). All changes verified: tsc clean, dev server 200 on all four pages, content spot-check passes.</rationale>
<parameter name="alternatives">(a) Skip the bench-card removal from home and just simplify the numbers — rejected; user asked "simplify or get rid of it" and the full /bench page already exists. (b) Keep the "free tier / cloud tier" framing but rename "cloud tier" to "optional cloud features" — rejected; ADR-003 explicitly kills the tier framing, not just the tier names. (c) Delete the cryptography section entirely — rejected; security-conscious devs still need the bearer-token detail, just tighter than what was there. (d) Split into multiple commits (hero+homepage / bench / security / changelog) — rejected; one user-driven UX pass, atomic commit is more honest about what shipped together.</parameter>
<parameter name="tags">["skein-website", "messaging", "apache-2.0", "ADR-003", "marketing"]
- Skein is locked as open source, free, Apache 2.0 — forever. The four-tier paid Business Model (Personal Pro $9 / Team $18 / Enterprise) dated 2026-05-08 is dead for Skein itself. Adjacent products with separate names may exist as separate businesses, but the Skein project — daemon, CLI, MCP server, embeddings, recall, all of it — has no paid tier and never will. License is Apache 2.0 (patent grant + permissive + corporate-friendly + package-manager-friendly). New distribution plan layers on top of the existing 4-phase Distribution Playbook: adds OSS-specific channels (Lobste.rs, awesome-X PRs, package managers, default-install bundling) and explicit sustainability mechanics (realistic scope cap — 12 visible CLI commands max, <600 tests, <5000 LoC; GitHub Sponsors + Open Collective for symbolic income; contributor pipeline as long-term survival path with ≥3 external contributors merged by Month 6; optional adjacent paid products like cloud-sync-with-separate-name, consulting, training; acquihire/foundation handoff as good endings). Founder survival: 8-week intense launch, then drop to ≤10 hrs/week regardless of trajectory; no support DMs ever, funnel to Issues/Discussions; career capital is the primary founder return. Documented in [[20 Projects/Skein/40_Decisions/ADR-003-open-source-free-forever]] and [[20 Projects/Skein/50_Marketing/OSS Distribution Plan]].
Alternatives considered: (a) Closed-source proprietary SaaS (Mem0-style) — rejected; the entire value proposition is being neutral context infrastructure between commercial AI tools, and a closed daemon reading everything from every coding agent is the opposite product. (b) Open core with paid Team/Cloud tier (the previous Business Model) — rejected; commercial pressure on the OSS core is the classic failure mode (GitLab, Sentry, MongoDB) where users learn to expect free-tier regression; for a neutrality product, every "upgrade to get X" eats trust capital; and the work of running paid infra (auth, billing, SSO, SOC2, support) is 3-6 months of distraction pre-PMF. (c) Source-available (BSL / Elastic-license style) — rejected; kills inclusion in package managers, kills corporate adoption via legal review, kills default-install bundling by other tools; protection-from-AWS-cloning argument doesn't apply to a local-first daemon. (d) Dual-licensed (GPL + commercial) — rejected; same package-manager and adoption costs as (c) plus dual-license dance is a maintenance burden for one person. (e) Foundation handoff (Apache, SFC, NumFOCUS) immediately — deferred; appropriate once there's a real user base and contributor pipeline (~1k stars, 5+ committers); revisit Month 12.
Rationale: Three reasons. (1) The product thesis demands it. Skein's claim is "trusted neutral context between every coding LLM." Any commercial agenda on Skein itself contradicts the claim. The moment a user wonders "is this fragment going somewhere?" or "will recall get worse in the free tier next year?", the product loses. Free + OSS + local-first + Apache-2.0 is the only configuration that makes the trust story unconditional. (2) Distribution wins. Free-forever OSS opens channels paid/source-available products can't reach: Homebrew core, Linux distro repos, awesome-X curation, default-install bundling by other tools, academic citation, conference acceptance, default suggestion in tutorials. These channels compound; the paid-tier roadmap can't access them. (3) Reversibility is asymmetric. Going from "free OSS" to "paid tiers later" is a community betrayal that has killed projects (Audacity, FreeRTOS-pro discontent). Going from "free OSS" to "donate to foundation and walk away" is honorable and clean. Lock in the harder-to-reverse direction now. The sustainability question (how does this not die from solo maintainer burnout in 18 months) is real but separable — addressed via scope cap, contributor pipeline, GitHub Sponsors, optional adjacent paid products, and time-boxed launch — not by paywalling Skein.</rationale>
<parameter name="tags">["distribution", "oss", "license", "apache-2.0", "free-forever", "ADR-003", "business-model", "sustainability"]
- Decided to use X` / `TODO: Y` / `remember that Z` style fragments to `skein inbox` for your review [tags: transcript, session:efd27ae9-4709-47bf-af5b-02948e7cdcb0, passive]

## Preferences

- Tests live in `tests/` (99 files). [tags: testing, layout]

## Current state

- **readme**: # Scopes

Scopes form a visibility hierarchy: `public ⊃ org ⊃ team ⊃ project ⊃ personal`.  
A recall query on `project:foo` returns fragments from that project, its team, org, and public.

Scopes are inferred automatically — `skein up` picks one from
`.skein/scope`, the git remote, or the directory …
- **readme**: # REST (same hybrid pipeline)

curl -X POST http://127.0.0.1:8765/v1/chunks/search \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"query":"auth bearer","scope":"project:myapp","limit":5}'
```

Results return file path + line range + the matched chunk content, ranked by hybrid BM25 + vector + RRF.

#…
- **readme**: # MCP — Claude Code, Cursor, etc. call this directly

search_code(query="how does auth work", scope="project:myapp")
- **readme**: # (e.g. after switching from gemini to fastembed):

skein doctor --reindex-embeddings
```

What gets ingested:

- **Languages auto-detected** by extension: Python, JS, TS, Go, Rust, Java, Kotlin, Swift, Ruby, PHP, C, C++, C#, Scala, Clojure, Elixir, Haskell, Lua, Shell, SQL, Markdown, YAML, JSON, TO…
- **readme**: # Re-embed every fragment under the active embedding provider
- **readme**: # Force a full re-index (replaces the old `skein ingest .`):

skein doctor --reingest
- **readme**: # First-time setup: indexes the cwd and starts the watcher.

skein up
- **readme**: # Codebase RAG

Fragments are great for typed context — decisions, requirements, observations. They're not great for "find the function that does X across this 50k-LOC codebase." For that, Skein has a separate **chunks** layer.

### Ingest a codebase

`skein up` runs the initial ingest and registers…
- **readme**: # Disconnect a client (removes the hooks):

skein connect cursor --remove
skein connect --all --remove   # disconnect everything
```

`skein up` writes the hooks into `.claude/settings.json` automatically (the per-project `.skein/scope` file pins which scope to use).

---
- **readme**: # What's wired up?

skein status
- **readme**: # Run once per project — installs hooks for every detected client:

cd ~/Documents/your-project
skein up
- **readme**: # Autonomous mode

`skein up` (which also runs on `skein connect`) turns Skein from "tools the LLM can call" into "context that flows automatically without anyone asking." It writes a small set of files into the current project:

| File | Purpose |
|---|---|
| `.skein/scope` | Pins which scope hooks…
*… 49 more state fragments — call `recall` for full context.*

<!-- @user -->
<!-- Add any manual context here. This block is preserved across `skein sync`. -->
<!-- /@user -->
